import 'package:flutter/material.dart';

// FinanceAI uses only these four categories in both the app and backend.
const List<String> financeCategories = [
  'Shopping',
  'Transport',
  'Bills',
  'Subscriptions',
];

class AppColors {
  static const purple = Color(0xFF6C4DFF);
  static const lightPurple = Color(0xFFF1EEFF);
  static const background = Colors.white;
  static const textDark = Color(0xFF1F2330);
  static const textMuted = Color(0xFF6B7280);
}
